import { useState } from 'react'
import PropTypes from 'prop-types'

import paymentService from '../services/payment'

const Payment = ({ ___ }) => {

}

Blog.propTypes = {
	___: PropTypes.object.isRequired,
}

export default Payment
